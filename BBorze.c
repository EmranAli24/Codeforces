#include<stdio.h>
#include<string.h>
int main()
{
	char str[201];
	int len;
	
	fgets(str,sizeof(str),stdin);

	len = strlen(str);

	for(int i=0;i<len;i++)
	{



/*	if(str[i]=='.')
		{
			printf("0");
		}

		if(str[i]=='-.')
		{
			printf("1");
		}

		if(str[i]=='--')
			{
				printf("2");
			}*/
	}

	return 0;
}
